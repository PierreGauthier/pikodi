
# - name: "Install {{item.name}} (1/3)"
#   get_url:
#     url: "{{item.url}}"
#     dest: "/home/osmc/.kodi/addons/packages/{{item.package}}"
# - name: "Install {{item.name}} (2/3)"
#   command: "unzip /home/osmc/.kodi/addons/packages/{{item.package}}.zip -d /home/osmc/.kodi/addons/"
#   args:
#     warn: false
- name: "Install {{item.name}} (3/3)"
  file:
    path: "/home/osmc/.kodi/addons/{{item.code}}"
    owner: osmc
    group: osmc
    mode: 0755
    recurse: true
# - name: "Install {{item.name}} (3/3)"
#   command: kodi-send --action="InstallAddon(plugin.video.youtube)"
